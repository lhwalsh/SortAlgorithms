// check out arraycopy
public class MergeSort implements SortAlgorithm {
    public void sort(double[] a, SortTimer t) {
	// code he showed us in class
	/*this.t = t;
	sort(a, 0, a.length - 1);*/
    }
    //This is code he showed us in class
   /* void merge(double[] a, int left, int mid, int right) {
	t.addComparison();
	if (a[mid] <= a[mid+1]) return;
	int p = left, q = mid + 1, fresh = 0;
	double[] aa = new double [right + 1 - left];
	while (p <= mid && q <= right) {
	    t.addComparisons()
    System.arraycopy(aa, 0, a, left, fresh); //copy merged elements back
    // copy aa[0..fresh-1] to a[left..left+fresh-1]
    t.addMoves(fresh);*/
}
